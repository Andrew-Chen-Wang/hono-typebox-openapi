// Intentionally (almost) empty.
//
// The Swift client is produced at build time by the OpenAPIGenerator build-tool plugin from
// `openapi.json` + `openapi-generator-config.yaml` in this directory. This file only ensures
// the target's source directory exists in version control even when no spec has been fetched
// yet (openapi.json is written by the integration test and is gitignored).
